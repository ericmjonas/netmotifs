import numpy as np

"""
Generate synthetic data

"""

def gen_synth():
    # there are JOB_C classes of jobs (call them fields)
    # there are USER_C classes of users (some users apply to some fields)
    # there are LOC_C distinct locations, jobs and users are randomly
    #    assigned to these locations, and a user mostly applies for jobs in his
    #    own city

    JOB_C = 10
    JOBS_PER_C = 50
    USER_C = 8 
    USERS_PER_C = 50 
    LOC_C = 16

    P_MYLOC = 0.9
    
    LOC_SCALE = 10.0

    for j in 
    
    
